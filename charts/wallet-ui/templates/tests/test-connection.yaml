apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wallet-ui.fullname" . }}-test-connection"
  labels:
    {{- include "wallet-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wallet-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
