apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "isbe-certauth.fullname" . }}-test-connection"
  labels:
    {{- include "isbe-certauth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "isbe-certauth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
