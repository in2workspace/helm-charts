apiVersion: v1
kind: Service
metadata:
  name: {{ include "eudistack-issuer-enterprise-backend.fullname" . }}
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    {{- include "eudistack-issuer-enterprise-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ include "eudistack-issuer-enterprise-backend.serverPort" . }}
      protocol: TCP
      name: http
  selector:
    {{- include "eudistack-issuer-enterprise-backend.selectorLabels" . | nindent 4 }}