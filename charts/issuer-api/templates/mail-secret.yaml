{{- if not .Values.mail.password.existingSecret.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: mail-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{ include "issuer-api.labels" . | nindent 4 }}
type: Opaque
data:
  mail-password: {{ .Values.db.password | b64enc }}
{{- end }}