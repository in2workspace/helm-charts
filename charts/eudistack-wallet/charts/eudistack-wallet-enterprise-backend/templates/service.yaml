apiVersion: v1
kind: Service
metadata:
  name: {{ include "eudistack-wallet-enterprise-backend.fullname" . }}
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    {{- include "eudistack-wallet-enterprise-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ include "eudistack-wallet-enterprise-backend.serverPort" . }}
      protocol: TCP
      name: http
  selector:
    {{- include "eudistack-wallet-enterprise-backend.selectorLabels" . | nindent 4 }}
