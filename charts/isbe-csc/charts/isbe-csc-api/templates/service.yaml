apiVersion: v1
kind: Service
metadata:
  name: {{ include "isbe-csc-api.fullname" . }}
  labels:
    {{- include "isbe-csc-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isbe-csc-api.selectorLabels" . | nindent 4 }}
