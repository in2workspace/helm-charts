apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "isbe-csc-api.fullname" . }}-test-connection"
  labels:
    {{- include "isbe-csc-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "isbe-csc-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
