apiVersion: v1
kind: Service
metadata:
  name: {{ include "wallet-keycloak.fullname" . }}
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    {{- include "wallet-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.internalServerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "wallet-keycloak.selectorLabels" . | nindent 4 }}
