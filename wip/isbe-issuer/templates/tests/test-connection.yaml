apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "isbe-issuer.fullname" . }}-test-connection"
  labels:
    {{- include "isbe-issuer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "isbe-issuer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
