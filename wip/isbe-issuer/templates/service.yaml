apiVersion: v1
kind: Service
metadata:
  name: {{ include "isbe-issuer.fullname" . }}
  labels:
    {{- include "isbe-issuer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "isbe-issuer.selectorLabels" . | nindent 4 }}
