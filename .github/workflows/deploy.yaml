name: Deploy Helm Charts

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  package-and-publish:
    name: Package and Publish Helm Charts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Helm
        run: |
          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          helm version

      - name: Authenticate to GitHub Container Registry (GHCR)
        run: echo "${{ secrets.GH_PAT }}" | helm registry login ghcr.io --username $GITHUB_ACTOR --password-stdin

      - name: Create Output Directory
        run: mkdir -p helm-packages

      - name: Package and Push Helm Charts
        run: |
          CHARTS=(
            desmos
            issuer-api
            dome-issuer-keycloak
            issuer-ui
            wallet-api
            dome-wallet-keycloak
            dome-wallet-ui
            wallet
            issuer
            dome-dss
            access-node
            verifier-api
            portal-ui
            keycloak
          )

          for CHART in "${CHARTS[@]}"; do
            if [ -d "charts/$CHART" ]; then
              echo "📦 Building dependencies for $CHART"
              helm dependency build "charts/$CHART"

              echo "📦 Packaging Helm Chart: $CHART"
              helm package "charts/$CHART" --destination helm-packages

              echo "🚀 Pushing Helm Chart to GHCR"
              helm push helm-packages/$CHART-*.tgz oci://ghcr.io/${{ github.repository_owner }}/helm-charts
            fi
          done

      - name: Generate index.yaml
        run: |
          echo "⚙️ Generating index.yaml..."
          helm repo index helm-packages --url oci://ghcr.io/${{ github.repository_owner }}/helm-charts
          cp helm-packages/index.yaml index.yaml

      - name: Upload index.yaml as an artifact
        uses: actions/upload-artifact@v4
        with:
          name: helm-index
          path: index.yaml
